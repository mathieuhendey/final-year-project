services:
  app.curl_wrapper:
    class: AppBundle\Service\CurlWrapper
  app.analysis_getter:
    class: AppBundle\Service\AnalysisGetter
    arguments: ['@app.curl_wrapper']
  app.home_page_controller:
    class:     AppBundle\Controller\HomePageController
    arguments: ['@templating', '@router', '@session']
